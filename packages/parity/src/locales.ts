// Parity's own copy of the locale list (cannot import from apps/web — separate workspace).
// Mirrors apps/web/lib/i18n/config.ts.
export const LOCALES = ['en', 'zh-hk', 'zh-tw', 'ja', 'ko', 'th', 'zh-cn'] as const
export const DEFAULT_LOCALE = 'en'
